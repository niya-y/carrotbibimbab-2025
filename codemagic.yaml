workflows:
  ios-release:
    name: iOS TestFlight Release
    working_directory: flutter_app
    environment:
      flutter: "3.35.5"
      xcode: latest
      cocoapods: default
      vars:
        FLUTTER_CHANNEL: "stable"
        XCODE_WORKSPACE: "ios/Runner.xcworkspace"
        XCODE_SCHEME: "Runner"
        BUNDLE_ID: "com.niya-y.testapp"        # ← 본인 Bundle ID
        APP_STORE_CONNECT_TEAM_ID: "F9HD79X8WM" # ← 본인 Team ID

    scripts:
      - name: Install dependencies
        script: |
          flutter pub get

      # 1) 서명파일 준비 + 수동 서명 강제 + 프로파일 주입 (빌드 전에!)
      - name: Fetch & apply signing
        script: |
          keychain initialize
          app-store-connect fetch-signing-files \
            --bundle-id $BUNDLE_ID \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          # 자동서명 방지: Runner 타겟에 Manual 강제
          xcode-project set-target-build-settings "Runner" CODE_SIGN_STYLE="Manual"
          # 정확한 타겟에 프로비저닝/서명 설정 주입
          xcode-project use-profiles \
            --project ios/Runner.xcodeproj \
            --scheme $XCODE_SCHEME \
            --archive-method app-store

      # 2) 서명된 상태에서 IPA 생성
      - name: Build iOS for release
        script: |
          flutter clean
          flutter pub get
          flutter build ipa --release \
            --dart-define=ENV=prod

    artifacts:
      - flutter_app/build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_API_KEY
        key_id: $APP_STORE_CONNECT_KEY_ID
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
